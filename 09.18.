#include <iostream>
using namespace std;

int F(int n)
{
    if (n > 2)
        return F(n-1) + F(n-2); 
    else
        return 1;
}

int main()
{
    int x = 35;
    cout << F(x) <<endl;
    return 0;
}
